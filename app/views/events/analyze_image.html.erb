<%= stylesheet_link_tag 'analyze_image', media: 'all', 'data-turbolinks-track': 'reload' %>
<header>
  <h1>画像分析結果 - <%= params[:vegetable_name] %></h1>
</header>
<div class="container">
  <% if @vegetable_status.present? %>
    <div class="alert alert-success" role="alert">
      <strong>野菜の状態：</strong>
      <span><%= @vegetable_status[:status] %></span>
    </div>
  <% end %>

  <% if @care_guide.present? %>
    <div class="alert alert-info" role="alert">
      <strong>育て方のポイント：</strong>
      <% if @care_guide[:watering] %>
        <p><strong>Watering:</strong> <%= @care_guide[:watering] %></p>
      <% end %>
      <% if @care_guide[:sunlight] %>
        <p><strong>Sunlight:</strong> <%= @care_guide[:sunlight] %></p>
      <% end %>
      <% if @care_guide[:pruning] %>
        <p><strong>Pruning:</strong> <%= @care_guide[:pruning] %></p>
      <% end %>
    </div>
  <% else %>
    <div class="alert alert-warning" role="alert">
      育て方のポイントが見つかりませんでした。
    </div>
  <% end %>

  <% if @vegetable_status.blank? && @care_guide.blank? %>
    <div class="alert alert-warning" role="alert">
      分析結果が見つかりませんでした。
    </div>
  <% end %>

  <div class="mt-3">
    <%= link_to '別の画像を分析する', new_analyze_image_path, class: 'btn btn-primary' %>
  </div>
</div>